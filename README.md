# Audit: Checks if any collection or endpoint is missing registrations

pnpm swagger:check

# Auto-Sync: Automatically detects and registers new collections & endpoints across all files

pnpm swagger:sync
